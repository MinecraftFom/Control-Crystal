package com.fom477.api.logic;

import net.minecraft.network.chat.Component;
import net.minecraft.server.level.ServerPlayer;
import net.minecraft.world.BossEvent;
import net.minecraft.world.entity.player.Player;

import java.util.HashMap;
import java.util.Map;

public class CCBars{
    /*private static final Map<ServerPlayer, BossEvent> bossBars = new HashMap<>();
    public static void createBossBar(Player _player, String _title, float _length)
    {
        BossBar bossBar = new BossBar(_title, _length);
        bossBars.put((ServerPlayer) _player, bossBar.getBossInfo());
    }*/
}

class BossBar {
   /* private final String name;
    private float health;
    private final float maxHealth;

    public BossBar(String name, float maxHealth) {
        this.name = name;
        this.maxHealth = maxHealth;
        this.health = maxHealth;
    }

    public void setHealth(float health) {
        this.health = health <= maxHealth ? health : maxHealth; // 确保生命值不超过最大值
    }

    public float getHealth() {
        return health;
    }

    public float getMaxHealth() {
        return maxHealth;
    }

    public BossEvent getBossInfo() {
        // 使用component来提供Boss名字

    }*/
}